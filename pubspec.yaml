name: image_to_pdf
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  cupertino_icons:
  flutter:
    sdk: flutter
  image_picker:
  intl:
  open_file:
  path:
  path_provider:
  pdf:

dev_dependencies:
  flutter_launcher_icons:
  flutter_test:
    sdk: flutter
  lint:

flutter_icons:
  android: "launcher_icon"
  ios: true
  remove_alpha_ios: true
  image_path: "assets/img/logo_img_to_pdf.png"

flutter:
  uses-material-design: true
  assets:
    - assets/img/
